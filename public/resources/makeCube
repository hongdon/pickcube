makeCube.js 는 큐브 생성에 관한 전반적인 것이 담겨있다.

큐브 생성의 논리는 333보다 더 높은 차수의 큐브 생성, 활용도 수월하게 만들었다.

배열 객체를 이용하여
배열안의 배열안의 리터럴 객체를 이용해 구현하였다.

ex)
[ [ {index=0, face="L", color="!!", marked=""} --- ---]]]

전체에서 0번째 배열의 0번째 요소의 객체 리터럴이 곧 해당 면의 정보이다.
 
 
 facesSetting : 면의 순서를 정하여 [[전체에서 ?번째 벼열의 ?번째 요소가 X면이다]]를 정한다. 기준은 LFRBUD 이다
 setEmpty :  위에서 정해 놓은것을 토대로 안에 세부 정보를 셋팅한다.(인덱스만 세팅되어있다.)
 colorStringParser : 면의 색 순서를 담는 배열을 리턴한다.
 setting : 그 배열을 가지고 색깔까지 셋팅하여 최종적으로 완성형 큐브를 생성한다.